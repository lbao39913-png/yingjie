import '../../widgets/phase_placeholder.dart';

class CategoryPage extends PhasePlaceholder {
  const CategoryPage({super.key})
      : super(
          title: '分类',
          summary: '分类浏览会在后续 Phase 接入 MediaApi.fetchCategories。',
        );
}
